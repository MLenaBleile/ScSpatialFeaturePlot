% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ScSpatialFeaturePlot.R
\name{ScSpatialFeaturePlot}
\alias{ScSpatialFeaturePlot}
\title{Spatial Feature Plot with Shared Color Scale}
\usage{
ScSpatialFeaturePlot(
  spat,
  features,
  slot = "counts",
  facet_labeller = NULL,
  logs = FALSE,
  pt.size.factor = 1.6,
  cells = Seurat::Cells(spat),
  LegLabel = "Count",
  uq = 0.999,
  lq = 0,
  flip = 3,
  ncol = NULL,
  nrow = NULL,
  combine = TRUE
)
}
\arguments{
\item{spat}{A Seurat object containing spatial transcriptomics data.}

\item{features}{Character vector of gene names or metadata column names to plot.}

\item{slot}{Character string specifying which data slot to pull from.
Default is \code{"counts"}.}

\item{facet_labeller}{A labeller function for facet labels. Default is \code{NULL}.}

\item{logs}{Logical; if \code{TRUE}, log-transform expression values using
\code{log(x + 1)}. Default is \code{FALSE}.}

\item{pt.size.factor}{Numeric; point size multiplier for the scatter plot.
Default is \code{1.6}.}

\item{cells}{Character vector of cell names to include in the plot. Default
is all cells in \code{spat}.}

\item{LegLabel}{Character string for the legend title. Default is \code{"Count"}.}

\item{uq}{Numeric; upper quantile for value clipping. Default is \code{0.999}.}

\item{lq}{Numeric; lower quantile for value clipping. Default is \code{0}.}

\item{flip}{Integer specifying rotation mode: 1 = flip x-axis, 2 = transpose
axes, 3 = flip y-axis. Default is \code{3}.}

\item{ncol}{Integer; number of columns in the faceted plot. Default is
\code{NULL} (automatic).}

\item{nrow}{Integer; number of rows in the faceted plot. Default is
\code{NULL} (automatic).}

\item{combine}{Logical; if \code{TRUE}, return a single combined faceted plot.
If \code{FALSE}, return a list of individual ggplot objects. Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object (if \code{combine = TRUE}) or a list of
  \code{ggplot} objects (if \code{combine = FALSE}).
}
\description{
An extension of Seurat's \code{SpatialFeaturePlot} that allows plotting
multiple genes on the same color scale, facilitating comparison of expression
magnitude across genes. Also supports rotation of tissue coordinates.
}
\examples{
\dontrun{
# Single gene
ScSpatialFeaturePlot(spat, features = "MLANA", pt.size.factor = 2)

# Multiple genes on the same color scale
ScSpatialFeaturePlot(spat, features = c("MLANA", "MITF"), pt.size.factor = 2)

# Independent scales per gene
plots <- ScSpatialFeaturePlot(spat, features = c("MLANA", "MITF"), combine = FALSE)
do.call(gridExtra::grid.arrange, c(plots, ncol = 2))
}

}
